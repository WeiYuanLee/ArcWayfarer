fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
httpx>=0.27.0
pydantic>=2.0.0
# cryptography 50's macOS wheel dynamically links OpenSSL 3.  PyInstaller can
# resolve that dependency to sslpsk-pmd3's older bundled OpenSSL, which makes
# the packaged backend fail before startup.  The 45.x macOS wheel bundles its
# compatible OpenSSL implementation and works alongside sslpsk-pmd3.
cryptography>=42,<46
# Keep development, local packages, and CI on the same verified RSD/DVT stack.
# 11.3.1 adds the current macOS native-tunnel and legacy Developer Disk Image
# fixes while remaining compatible with the cryptography 45.x packaging cap.
pymobiledevice3==11.3.1
packaging>=23.0
gpxpy>=1.6.0
pyimg4>=0.1.0
qrcode[pil]>=7.4
