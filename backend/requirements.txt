fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
httpx>=0.27.0
pydantic>=2.0.0
# cryptography 50's macOS wheel dynamically links OpenSSL 3.  PyInstaller can
# resolve that dependency to sslpsk-pmd3's older bundled OpenSSL, which makes
# the packaged backend fail before startup.  The 45.x macOS wheel bundles its
# compatible OpenSSL implementation and works alongside sslpsk-pmd3.
cryptography>=42,<46
# Keep the frozen backend aligned with the development environment.  A broad
# range let fresh CI builds install pymobiledevice3 10.x while local startup
# used 9.40.0, producing different USB discovery behaviour.
pymobiledevice3==9.40.0
packaging>=23.0
gpxpy>=1.6.0
pyimg4>=0.1.0
qrcode[pil]>=7.4
