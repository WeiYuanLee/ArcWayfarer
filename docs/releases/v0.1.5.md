## 本次更新

- 修正：macOS 首次開啟時出現「Backend server failed to start」的問題（延長啟動等待時間至 60 秒）
- 改善：啟動失敗時顯示 Gatekeeper 解決步驟，引導使用者前往「系統設定 → 隱私權與安全性」允許開啟
- 改善：DMG 檔名現在明確標示架構（`x64` / `arm64`），方便 Intel 與 Apple Silicon 使用者辨識

---

## What's new

- Fixed: "Backend server failed to start" on first macOS launch (startup wait extended to 60 seconds)
- Improved: Startup failure dialog now shows step-by-step Gatekeeper instructions (System Settings → Privacy & Security → Open Anyway)
- Improved: DMG filenames now include architecture (`x64` / `arm64`) so users can easily pick the right download
